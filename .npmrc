legacy-peer-deps=true
node-linker=hoisted
# Suppress deprecation warnings from transitive dependencies during install.
# These originate from drizzle-kit (@esbuild-kit/*) and electron-builder (global-agent/boolean)
# and do not affect functionality. Errors remain visible.
loglevel=error
fund=false
