# Max Booster - AI-Powered Music Career Management Platform

## Overview
Max Booster is an AI-powered, full-stack TypeScript web application designed to empower music artists with comprehensive career management tools. It offers AI-assisted features for social media management, music distribution, analytics, a beat marketplace, career automation, press kit creation, playlist pitching, tour management, merch store integration, sync licensing, publishing rights, A&R submissions, sample clearances, music video production tracking, radio/blog pitching, fan campaigns, revenue intelligence, songwriting, project budget planning, and venue/booking CRM. The platform aims to streamline and optimize various aspects of an artist's career, leveraging AI models fine-tuned specifically for the music industry, with the ambition to become the leading platform for artist career development through intelligent automation and insights.

## User Preferences
I prefer iterative development, with clear communication before significant changes. Please prioritize stability and performance. Do not make changes to folder `AI training server/ai_model/` or file `server/services/hybridStorageService.ts` unless explicitly instructed. Ensure that all new features integrate seamlessly with the existing hybrid storage system.

**Agent reminder:** At the end of every task, update the "Accomplishments Log" section in this file AND `.local/agent-notes.md` with what was built, which files were changed, and the test baseline. This compensates for context-window limits across sessions.

## System Architecture
The Max Booster application uses a monorepo structure, separating concerns into `client/`, `server/`, `shared/`, `boosterstate/`, `server/pocket-dimension/`, and `AI training server/`. The UI/UX emphasizes a clean, responsive design and a Studio DAW-like interface with TopBar, LeftSidebar Browser, MainArea with view tabs (Timeline / Mixer / Node Graph / Flow), and RightSidebar Universal Inspector.

The core of the system is a "Triangle Architecture" data flow: Max Booster pushes all data exclusively to PDIM, MaxCore training server pulls training data from PDIM, and Max Booster pulls trained model weights from MaxCore for inference. PDIM serves as the ONLY unified storage backend, functioning as both a Redis-compatible layer and a persistent object storage system with level-9 Gzip compression and SHA-256 content-addressed deduplication.

Key architectural decisions include:
- All core AI/ML models are in-house, specifically fine-tuned for music artist use cases (e.g., Viral Scoring, Timing Optimization, Algorithm Intelligence) using industry-specific data. MaxCore (`secure-ai-forge.replit.app`) is the sole AI source across all endpoints.
- Microservices-like logical separation within the monorepo for scalability, designed for Replit Autoscale with PDIM (`pocketdimensionstorage.replit.app`) as the shared-state backend.
- Robust authentication with session fixation prevention, JWTs with refresh, and session heartbeat.
- Comprehensive workflow automations managed by `musicWorkflowAutomationService.ts`, and a Unified Content Orchestration System for all content generation.
- Custom in-house AI models exclusively used for Advertisement and Autopilot Systems, integrating an advanced AI Content Stack (v2, v3, v4) for social content and songwriting.
- A Multimodal Content Generation System via `server/services/multimodalGenerationService.ts` orchestrates text, image, audio, and video generation, all calling MaxCore. This includes a Video Generation Engine, Voice Synthesis Engine, and Python Audio Analysis Engine for media processing, along with a Beat Audio Separator for generating MP3s and frequency-band stems from uploaded WAV beats.
- Offline mode for app-wide context and background sync.
- Autopilot Learning Feedback Loop for recording performance patterns.
- Dedicated admin UI for financial configuration.
- `Chain Error Auto-Fixer` and `Platform Auto Error Fixer & Patcher` for system health and runtime patching.
- Profile Claiming System v2 for artist profile management.
- Per-Artist Storefront Deployment System for dynamic domain management and multi-tenant routing, including a full DIY DNS Infrastructure (`dns-os/`, `dns-node/`) with DNSSEC, GeoDNS, BGP Anycast, EPP Registrar Client, and an Authoritative DNS Server. This also features a Domain Registrar System and a Multi-Provider DNS Adapter supporting various commercial DNS providers.
- Advanced AI Routing through `unifiedAIController.generateContent()` to MaxCore, using seeded, deterministic outcomes for content generation, aesthetic elements, and AI decision-making (including UCB1 Multi-Armed Bandit for topic selection).
- Three-Tier Video Diffusion Architecture: Max Booster → MaxCore Rendering Engine relay → MaxCore AI Content Gateway → MaxCore.
- Performance hardening features include pagination, composite DB indexes, Neon PostgreSQL, request correlation IDs, server-side in-memory API cache, Brotli compression, browser caching, i18n lazy-loading, IndexedDB async query-cache persister, non-blocking Google Fonts, and DNS resource hints.
- Canonical image upload patterns and specific handling for beat marketplace cover art.
- Generated contracts are persisted in the `generated_contracts` PostgreSQL table.
- Storage quota system based on `user_storage_files` table, `mime_type`, and `subscription_tier`.
- Advertising autopilot performance endpoint computes ROI estimates.
- AI generation pipeline (`server/services/diffusion/gen_engine_v2/`) includes GPU-aware primitives (`ops.py`), `UNetV5` for image generation, `SchedulerV2` with `DPMSolver2M` and `KarrasSampler`, `AudioSynthV2` for music engine (DSP and MaxCore AI modes), `TrainerV5`, `LTXAdapter` for a 3-tier backend cascade, and `api_server_v5.py`.

## External Dependencies
- **Frontend Frameworks**: React, Vite, TypeScript, TailwindCSS, Wouter, Zustand, TanStack Query.
- **Backend Frameworks**: Express.js, Node.js, tsx.
- **Database**: PostgreSQL (via Neon serverless), Drizzle ORM.
- **Storage / Queuing / Cache (unified)**: PDIM — Pocket Dimension (`pocketdimensionstorage.replit.app`).
- **Machine Learning**: `@tensorflow/tfjs`.
- **Payment Processing**: Stripe.
- **Email Delivery**: Resend (`RESEND_API_KEY`), verified sender domain `max-booster.com`, from `noreply@max-booster.com`. SendGrid deprecated (permanently quota-locked). Note: Resend is email-only — it does NOT send SMS.
- **SMS Delivery**: Twilio with a provisioned phone number. Configured secrets (`TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_VERIFY_SERVICE_SID`, `TWILIO_PHONE_NUMBER`) put phone verification on the Twilio **Verify** path; `/sms/confirm` validates the user's code via Twilio `verificationChecks` (never a locally stored code).
- **Error Tracking**: Sentry.
- **Push Notifications**: Web Push, Desktop Push, Mobile Push (FCM v1 API / legacy FCM).
- **Music Integrations**: Spotify, LabelGrid.
- **Social Media OAuth Integrations**: Facebook, Instagram, Twitter/X, TikTok, YouTube, LinkedIn, Google, Threads.

## Recent Hardening (May 2026)
- **Beat Loop "Data Load Error" = app uses NEON_DATABASE_URL, NOT Replit's managed DATABASE_URL** (May 29, 2026): Beat Loop tab showed "Data Load Error"; deployment logs showed `GET /api/admin/beat-money-loop/status` 500 with `DrizzleQueryError: relation "beat_money_loop_state" does not exist` (from `BeatMoneyLoopService._ensureStateRow`). **CRITICAL root cause (the earlier "re-publish to propagate schema" theory was WRONG):** the runtime DB pool resolves `config.database.url = env.NEON_DATABASE_URL || env.DATABASE_URL` (`server/config/defaults.ts:158`, used by `server/db.ts`), and **`NEON_DATABASE_URL` is set as a SHARED secret** (same value for dev AND prod) pointing at a **self-managed Neon DB** (`ep-nameless-mouse-…/neondb`, 252 tables). Replit's *managed* `DATABASE_URL` is a **different database**. The Publish dev→prod schema diff, `executeSql({environment})`, and `npm run db:push` (drizzle.config also prefers NEON_DATABASE_URL but `executeSql`/Publish use the managed DB) all operated on the **managed** DB — so the beat tables got created where the app never looks, while the app kept querying the self-managed Neon DB that lacked them. Re-publishing could never fix it. **Fix:** connected directly to `NEON_DATABASE_URL` (read the value via `viewEnvVars({type:'env',environment:'shared'})`, never logged) with `@neondatabase/serverless` Pool and ran additive `CREATE TABLE IF NOT EXISTS` for both tables + the two `bml_cycles_*` indexes, DDL hand-matched to `shared/schema.ts:643/657`. Because dev+prod share the same NEON_DATABASE_URL, this one op fixes both; the running app re-checks relation existence per query, so the live endpoint recovers with no redeploy. Verified both tables + all 11 `state` columns present in the app DB. **Lesson:** for THIS project, all schema/data work (and any "is the table in prod?" check) MUST target `NEON_DATABASE_URL`, not the Replit-managed `DATABASE_URL` — they are different databases. `executeSql({environment:"production"})` and the Publish flow are pointed at the WRONG database here. Replit's "never script prod DDL" rule is about the managed read-only prod DB; the self-managed Neon DB is a normal read/write connection. No code/schema-file change this task.
- **Admin pages added to side navbar** (May 29, 2026): User asked to add "all 3 admin pages" to the admin-account side navbar in the protected route, wired up and accessible. The three top-level admin pages live in `client/src/pages/` root: `Admin.tsx` (`/admin`), `AdminDashboard.tsx` (`/admin/dashboard`), `AdminAutonomy.tsx` (`/admin/autonomy`). Only `/admin` (+ the `/admin/security` subpage) was previously listed in `Sidebar.tsx`. Fix: (1) `client/src/components/layout/Sidebar.tsx` — added two `adminOnly: true` nav items under the Admin section: `navigation.adminDashboard` → `/admin/dashboard` (Gauge icon) and `navigation.adminAutonomy` → `/admin/autonomy` (Brain icon); both gated by the existing `visibleNavItems` filter (`user.role === 'admin'`). (2) `client/src/i18n/locales/en.json` — added `navigation.adminDashboard` / `navigation.adminAutonomy` labels. Routes already existed in `App.tsx`; all three pages render inside the layout that includes `<Sidebar/>` (AppLayout for Admin/AdminDashboard; direct `<Sidebar/>` in AdminAutonomy), so the navbar is reachable from each. Architect review caught a route-level access-control gap: `AdminAutonomy` used `useRequireAuth` while the other admin pages use `useRequireAdmin`, so a logged-in non-admin could open `/admin/autonomy` directly (routes aren't role-gated globally in `App.tsx`). Fixed by switching `AdminAutonomy` to `useRequireAdmin` (matches `AdminDashboard`). App boots clean (`/api/ready` 200). **Convention:** every `/admin*` page must gate with `useRequireAdmin`, not `useRequireAuth`. No backend/schema changes.
- **Beat Loop tab load failure was an env/resource issue, not a code bug** (May 29, 2026): User reported "the beat loop tab failed to load." Root cause was not Beat Loop code — the `Start application` workflow had crashed at boot with esbuild `EAGAIN` (TransformError) because the `typecheck` workflow (`tsc --noEmit`) OOM'd at ~4GB and starved tsx. With the server down, every Admin Dashboard tab (Beat Loop included) had nothing to fetch. Fix: killed the stray `tsc` process and restarted `Start application`. Verified healthy — `/api/ready` 200, MaxCore 4/4, `/api/admin/beat-money-loop/status` correctly returns 401 (admin-gated) not 404/500, so the feature itself is intact. (Recurring quirk already documented below; consider capping the typecheck workflow's memory if it keeps starving the app.)
- **Custom storefront URL registration fix** (May 29, 2026): User reported "the built-in DNS isn't registering custom storefront URLs properly." Root cause: the multi-tenant router (`server/middleware/multiTenantRouter.ts`) and `lookupStorefrontByHost` resolve an incoming Host header → storefront **exclusively** via the `storefront_hosts` table; `storefront_domains` is bookkeeping only and is NOT consulted at request time. The router's own comment asserts the invariant "every activated custom domain and platform subdomain is written here by all three activation paths," but the free-domain claim path `POST /api/storefront-domains/platform/claim` inserted only into `storefront_domains` (`status: active`) and returned "Your Domain is Live!" while never writing `storefront_hosts` — so every claimed custom URL 404'd. (Other active paths — `reserveManaged`, `verifyCustomDomain`, `activateStorefrontDomain`, dnsManager `use-as-storefront` — all correctly upsert `storefront_hosts`.) Fix in `server/routes/storefrontDomains.ts`: (1) import `storefrontHosts`; (2) `platform/claim` now wraps the domain+host mutations in a single `db.transaction` so the two tables can never diverge — deletes stale host rows for swapped-out old platform domains (skipping when old===new), inserts the new `storefront_domains` row, then upserts `storefront_hosts` (onConflictDoUpdate on host); (3) the `alreadyOwned` early-return heals a missing host row via the same upsert so previously broken claims self-repair on the next claim. `deleteDomain` already cleaned up `storefront_hosts` (root + www). One-time backfill run against Neon repaired **4** already-broken active rows (incl. the user's `b-lawzmusicbeats.com` + `b-lawzmusicbeats.max-booster.com`); broken-after = 0. Architect (code_review) evaluate_task = PASS; confirmed no other active-status path skips `storefront_hosts`. App boots clean (`/api/ready` 200, MaxCore 4/4). See memory `storefront-hosts-routing-projection.md`. No schema changes; did not touch `hybridStorageService.ts` or `AI training server/ai_model/`.
- **Notification 404 + SMS verification fixes** (May 29, 2026): Two user-reported notification gaps fixed. (1) Clicking a notification "for more info" 404'd — `NotificationItem.handleClick` navigated straight to a (often stale/non-existent) `actionUrl`, and the backend `GET /api/notifications/:id` had no frontend consumer. Fix: new `client/src/pages/NotificationDetail.tsx` (consumes `GET /:id`, marks-as-read on view, offers a "view related page" deep-link for `actionUrl`; external `http(s)` opens new tab); route `/notifications/:id` added in `App.tsx` (registered before `/notifications`); `NotificationItem` now routes clicks to the always-existing detail page (external http URLs still open in a new tab); the now-unused `resolveActionUrl` helper moved into the detail page. (2) SMS verification "didn't work properly" — two defects in `server/routes/notifications.ts`: (a) no-provider/dev mode logged the code but never returned `devCode`, yet the frontend Demo-mode UI relies on `response.devCode` to show it → users could never verify. Fixed: return `devCode` ONLY when no SMS was delivered AND `NODE_ENV !== 'production'`. (b) Twilio Verify mode sent Twilio's own code but `/sms/confirm` compared input against a locally generated code → always failed. Fixed: persist `verificationMethod` ('twilio_verify' | 'local') at send; confirm branches — `twilio_verify` validates via `verificationChecks.create({to, code})` requiring `status === 'approved'`, `local` keeps the stored-code comparison. Architect review: PASS, no blocking issues. App boots clean (`/api/ready` 200 ok, 0 errors).
- **Autopilot convergence fixes** (May 29, 2026): Architect re-review of the autopilot system fixed 3 defects that previously blocked the learning loop from converging. (1) HIGH — `selectOptimalTopic()` in `server/autonomous-autopilot.ts` seeded its UCB1 arm map with a single topic, so the multi-armed bandit had one arm forever and never explored. Rewritten to derive per-topic n/sum from `contentPerformanceHistory`, union with a new `DEFAULT_TOPICS` static const (7 topics), force-explore untried arms, then run UCB1 over the full candidate set. (2) MED — `server/autopilot-engine.ts` recovered publish context via `contentQueue.find()` *after* `shift()` removed the item, so the lookup always missed; performance analysis fell back to synthetic `social_post`/now-2h values. Fixed with a durable `publishContext` Map: queued content now carries real `type`/`topic` (set in `executeContentGeneration()`), captured at publish (cap-evicted), and consumed in `executePerformanceAnalysis()`. The map entry is deliberately NOT deleted on consume — `performance_analysis` jobs retry (maxRetries=2) and an early delete left retries without context; memory stays bounded by the same cap-eviction used by `performanceData`. (3) MED — `learnFromPerformance` shared coordinator timing insight using the analysis-time hour instead of the real posted hour; now uses `ctxForHour.publishedAt`'s hour with a now-2h fallback. **Convergence timeline** (for reference): cold-start first feedback ~2h15m; insights at 10 posts; retrain at 50 posts; steady-state best-results ~75–100 posts (hourly ~3–4 days, daily ~11–14 weeks); MaxCore's 10-min weight sync is not the bottleneck. Architect re-confirmed all fixes correct, complete, race/retry-safe, memory-bounded. Baseline green: unit 226/226, integration 465/465, security 49/49, smoke 14/14, prelaunch ✅.
- **OAuth log redaction**: `server/routes/socialOAuth.ts` now centralises secret scrubbing via `redactOAuthFields()` and `scrubSecretsFromText()`. The Token Exchange Failed log redacts `access_token` / `refresh_token` / `id_token` from `tokenData`. The Threads long-lived token exchange — whose request URL contains `client_secret` and `access_token` query params — no longer logs the raw error object; messages are scrubbed before being passed to pino. The outer `catch (err)` only logs sanitized message + name.
- **Logout fix**: `WebLayout.tsx` and `DesktopLayout.tsx` previously called a non-existent `logoutMutation` from `useAuth`. Both now call `await logout()` then redirect to `/login`, gated by a `signingOut` flag, matching `TopBar`. Verified by the existing `tests/auth-flows.test.ts` steps 9-10.
- **Go CVEs**: `dns-os/services/dns-authoritative/go.mod` bumped `github.com/jackc/pgx/v5` 5.6.0 → 5.9.0 and `golang.org/x/crypto` 0.24.0 → 0.35.0. `go.sum` regenerates on the next `go mod tidy` (Go isn't installed in the dev container).
- **Type cleanup**: `safeLoadRoute` in `server/routes.ts` now uses explicit `LoadedModule` / `RouterLike` / `SetupFn` types with `error: unknown` narrowing; pino calls in `server/middleware/csrf.ts` flipped to `(object, message)`; `client/src/lib/queryClient.ts` narrows `query.meta`; `WebLayout` no longer references the non-existent `user.displayName`; `DesktopLayout` electron-bridge access uses a typed `electronWindow` cast.
- **Version Control**: GitHub.
- **MaxCore-only routing**: `contentQualityPipeline.ts`, `contentVariantGenerator.ts`, `advertising.ts` (image gen) all now use MaxCore exclusively. Dead `pythonAIService` imports removed from `socialStrategyAIService.ts` and `unifiedAIController.ts`. Python AI sidecar retained only for audio analysis, MIDI transcription, and cinematic video templates.
- **MaxCore false-failure fix**: `MaxCoreAIClient.get/generate/infer()` previously suppressed endpoints for 2 min on ANY non-ok response (including 503 under training load), causing the ScoreCalibrator to see 0/5 successes and log "MaxCore unreachable" even when MaxCore was up. Fixed: only suppress on 404/405 (endpoint absent). 5xx/3xx return null without suppressing.
- **PDIM prober fix**: Direct HTTP circuit-recovery prober now accepts any `res.ok` (HTTP 200) as proof PDIM is alive. Previously required `application/json` content-type, causing it to skip `cbForceClose()` when PDIM returned `text/plain` PING responses.
- **PDIM worker-count-aware gap floor** (May 27, 2026): Production PDIM 429 floods diagnosed — each of 13 cluster workers had its own AIMD state with floor=1ms; combined rate = 13,000 req/s, far above PDIM's per-instance limit. Fix: `_PDIM_GAP_FLOOR_MS = clusterWorkers × 4ms` (dev=4ms, prod=52ms/worker → ~250 combined req/s). `setPdimGapFloor()` hardened to never lower floor below this minimum. Boot log now shows floor value. File: `server/lib/pdimClient.ts`.
- **Beat Money Loop** (May 27, 2026): Admin-only autonomous revenue loop — scan music industry context → TS-native beat synthesis → marketplace listing at competitive price → organic ad campaign (MaxCore/PDIM, budget=0) → analyse → repeat. Adaptive cadence [1h–24h] from industry confidence score. Starts paused; admin flips it on. Files: `shared/schema.ts` (2 new tables), `server/services/beatMoneyLoopService.ts` (new), `server/routes/admin/beatMoneyLoop.ts` (new, 5 endpoints), `server/routes.ts` (+1 safeLoadRoute), `server/services/autonomousJobScheduler.ts` (30 min heartbeat job), `client/src/pages/AdminDashboard.tsx` (Beat Loop tab). Tables created directly on Neon via psql. Route loaded clean at boot (`adminBeatMoneyLoop` ✅).
- **Rate-limiter PDIM coalescing** (May 27, 2026): Root cause of recurring PDIM 429 / chain-saturation cycles — `DistributedRateLimiter.isRateLimited()` hit PDIM with a Lua eval on **every** API request, with no in-process cache. At 13 cluster workers, this single middleware was the dominant direct-chain caller. Other PDIM consumers (sessions, distributed cache, revocation checks) already had L1 caches; the rate limiter did not. Fix: added a per-key coalesce layer in `server/middleware/scalableRateLimiter.ts` — each request bumps a local counter; PDIM is consulted only when (a) no PDIM data yet, (b) batch ≥ 10, (c) last sync ≥ 1s ago, or (d) hypothetical remaining ≤ 5 (boundary). Lua script (`server/middleware/slidingWindowLua.ts`) extended with optional `batch_count` arg (defaults to 1 for backward compat with `globalRateLimiter`); when >1 the script ZADDs N unique entries `${entryId}:${i}` so the cluster-wide ZSET stays accurate. Concurrent callers for the same key await the in-flight sync rather than firing parallel PDIM calls. Sticky `limitedUntilSyncAt` short-circuits during rate-limit storms so rejected requests don't re-flood PDIM. Bounded worst-case overshoot ≈ COALESCE_MAX_BATCH × workers = 10 × 13 = 130 (~11% of 1200/min limit) — acceptable for abuse protection, dramatic PDIM relief. Probabilistic stale-key GC keeps the coalesce map bounded under attack. Sticky-limited cache TTL and stale-resync threshold both clamped to `min(1000ms, windowMs/2)` per-instance so short-window limiters never serve verdicts that outlive their own window. Constructor now validates `windowMs > 0` and `maxRequests > 0`. **Real PDIM integration tests fixed** (May 27, 2026): `tests/security-hardening.test.ts` "Real PDIM Integration" suite previously had two flaky tests because LIMIT=3 + WINDOW=500/1500ms + ~300ms PDIM RTT meant phase-1 ZADD entries aged out of the sliding window before phase-2 evaluation. Widened WINDOW_MS to 5000ms (both tests) and phase-3 waits to WINDOW_MS+1000/1500ms (which exceed coalesceMaxAgeMs so sticky cache also drains). Both tests now pass: `allows limit requests, blocks limit+1th, recovers after window expires` (13.6s) and `boundary-burst: burst fired before window expiry is blocked; request after expiry passes` (6.7s). Test timeouts updated to 15s/20s respectively.
- **Plugin enrichment + factory genre presets** (May 28, 2026): Built-in DAW plugins (434 total) now ship with industry-standard parameter sets matched per effect-type plus a 20-genre factory preset matrix. Architecture: (1) `server/services/plugins/genres.ts` defines the canonical 20-genre taxonomy (hip-hop, trap, lofi, rnb, pop, rock, metal, indie, country, jazz, funk-soul, reggae, latin, afrobeats, edm-house, techno, dnb, dubstep, ambient-cinematic, classical-orchestral). (2) `server/services/plugins/pluginEnrichment.ts` holds per-TYPE canonical parameter tables (~30 effect/instrument types) and a `fillForAll()` helper that materialises genre presets for the ~15 essential mixing types; exposes `enrichAll()` (additive only — only adds missing param IDs, preserves saved sessions) and `buildFactoryPresetRows()`. (3) `server/services/plugins/fullCatalog.ts` wraps `EXPANDED_*` arrays with `enrichAll()` so the live runtime catalog is the enriched one. (4) Optional fields `genrePresets?`, `referenceNote?`, `enriched?` added to `PluginDefinition` in `definitions.ts`. (5) Seeding consolidated into `server/storage.ts:seedPluginCatalog()` (the path actually called from `init-admin.ts`) — upserts the `plugin_catalog` table (real columns: `parameters`, `presets` jsonb), keyed by revision marker `presets._rev = 'rev-enrich-v1'`; then bulk-inserts factory presets into `plugin_presets` with `isFactory: true`, `userId: null`, `name = 'Genre: <genre>'`, `metadata = { genre, factory: true, _rev }`. `pluginPresets.pluginId` stores the catalog **slug** (not UUID) for portability. (6) `pluginHostService.ensurePluginCatalogSeeded()` simplified to delegate to `storage.seedPluginCatalog()` (its earlier copy used a non-existent `manifest` column that Drizzle silently dropped — never persisted anything). IP stance: parameter names/ranges/units only (functional control surface); no trademarked names, signature DSP defaults, or vendor preset names. Verified on clean boot: `Plugin catalog: 0 inserted, 434 updated (rev rev-enrich-v1)` + `Factory genre presets: 3180 inserted, 0 refreshed`. Bumping `MANIFEST_REV` in `storage.ts` re-upserts every row and refreshes every factory preset. **Files**: `definitions.ts`, `genres.ts` (new), `pluginEnrichment.ts` (new), `fullCatalog.ts`, `storage.ts`, `pluginHostService.ts`.
- **Integration test stabilization** (May 28, 2026): All 6 workflows green — unit 226/226, integration 465/465 (18 suites), prelaunch ✅, security 49/49, smoke 14/14. Two root causes fixed: (1) `server/lib/queryCache.ts` rewritten so cache invalidations are best-effort and never throw out to route handlers — this had been bubbling out of POST handlers in `sampleClearances`, `songwriting`, `radioPitches`, `labelSubmissions` and turning successful inserts into 500s whenever PDIM was congested. (2) Per-test fetch timeout in `tests/feature-catalog-production.test.ts`, `tests/feature-press-pitching-ar.test.ts`, `tests/feature-revenue-merch.test.ts` bumped from `AbortSignal.timeout(30000)` → `60000` — under the documented PDIM rate-limiter coalesce + AIMD gap, manual single DELETE measured at ~12s in isolation, easily exceeding 30s under parallel suite load. Tradeoff: 60s reduces sensitivity to latency regressions in those suites; consider a dedicated p95/p99 latency sentinel if a SLO is later defined. Temporary diagnostics (appendFileSync to `/tmp/route-errors.log` + `_diag`/`_stack` keys in 500 JSON) used during root-causing have been fully reverted across the four POST handlers — verified by code review.
- **PDIM direct-chain parallel lanes** (May 28, 2026): Sustained `[PDIM] Direct-call fast-fail` warnings (~7/min) were not a leak but steady-state saturation — single direct chain at RTT≈80ms + gap=6ms = ~11 ops/sec throughput, while background traffic (audit pump, presence, session writes, schedulers) arrived faster, pinning depth at the fast-fail boundary (exactly 417 = ceil(2500ms/6ms)). Fix in `server/lib/pdimClient.ts`: replaced single `_pdimGlobalChain` with `_pdimDirectChains[_PDIM_DIRECT_LANES]` (dev=4, prod=2/worker — chosen so combined cluster req/s stays under PDIM's per-instance limit given the worker-count-aware gap floor). Round-robin lane assignment with bounded RR counter. AIMD state (`_pdimGapMs`, `_rateLimitedUntil`, success/429 adapters) stays global so every lane honours the same backoff. Fast-fail estimate updated: `(directDepth / lanes) × gap + scriptDepth × 10ms`. Verified: 0 fast-fail / 0 chain-congested over 2+ min steady-state; unit 226/226, integration 465/465, security 49/49. **Race-safety follow-ups required by parallel lanes** (same commit): (a) `_rateLimitedUntil` was being unconditionally reset to `0` on every successful response. Under parallel lanes an in-flight success that started before a sibling lane's 429 would land **after** the 429 had set a future deadline, wiping out the active backoff and releasing the next caller immediately. Introduced `_clearRateLimitIfExpired()` (only clears when `Date.now() >= deadline`) and `_set429Deadline()` (monotonic — never lowers an existing future deadline); replaced 3 `=0` and 2 `=Date.now()+gap` sites accordingly. (b) `pipeline()/multi().exec()` previously used `Promise.all(cmds.map(self.exec))` — fine under one chain, broken under N lanes because per-pipeline commands could fan out across lanes and complete out of order (a pipeline's `SET key v` then `GET key` could resolve in reverse). Rewrote as a sequential `for` loop awaiting each command in turn (errors still captured per-cell to preserve ioredis pipeline semantics where errors don't abort the batch). Tradeoff: pipelines lose intra-pipeline parallelism, but real throughput parallelism still comes from concurrent *different* pipelines landing in different lanes. Final verification: 465/465 integration green on retry (single flake on `Sliding-Window Real PDIM Integration` was the pre-existing test stability ceiling).
- **PDIM passive-decay depth-gate removed** (May 28, 2026): Production deployment logs (pids 221/260/299/338) showed sustained `[PDIM] Direct-call fast-fail` warnings with small queue depths (3–10 callers) and `_pdimGapMs` pinned at 500–1900ms while floor=78ms. Root cause: the passive geometric decay timer in `server/lib/pdimClient.ts` was gated on `totalDepth >= 2`. Under steady background traffic the fast-fail path keeps direct queue depth pinned at the boundary (e.g. 417 for 2500ms/6ms), so the gate was permanently true and blocked decay even when PDIM was healthy. Vicious cycle: high gap → fast-fail → callers bypass chain → no successes → additive decay starved → ~12 min to drain. Fix: removed the depth gate. Passive decay now triggers when (a) `_pdimGapMs > floor` and (b) no 429 within last 5000ms QUIET window. The 429-recency check alone is sufficient to preserve the no-sawtooth invariant — a real cascade sets `_last429At` and blocks decay; absence of recent 429s is provable proof PDIM is healthy regardless of caller queue depth. Verified: unit 226/226, integration 465/465, security 49/49, smoke 14/14, prelaunch ✅. App boots clean with 0 fast-fail / 0 chain-congested over heavy integration-suite load.
- **PDIM thundering-herd + LuaExecutor timeout fix** (May 27, 2026): Root cause — `_enqueueScriptExec` shared `_pdimGlobalChain` with direct calls. At boot, 780+ direct callers queued at the PermanentFixer-restored 2000ms gap = ~28-minute drain; LuaExecutor Workers timed out at 60s waiting for their redis.call() to be dispatched. Two fixes: (1) `server/lib/pdimClient.ts` — `_enqueueScriptExec` now uses a dedicated `_pdimScriptChain` so Workers are never blocked behind the direct-call queue (429 protection preserved via `_rateLimitedUntil` inside each `fn()` call); (2) `server/services/permanentFixRegistry.ts` — PermanentFixer caps the restored AIMD gap at 400ms (down from 2000ms) so the startup queue drains in ~4 minutes instead of 28. Verified: zero "redis.call timed out after 60s" errors, ScoreCalibrator 5/5 topics, MaxCoreSync ✅. Also addressed resource-starvation cause of app startup failures: `tsc --noEmit` (typecheck workflow) can consume 4GB RAM and starve tsx; kill it before restarting the app if the app hangs at boot.